# RBAC System Architecture & Diagrams

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         WasteFreeBD App                          │
│                     (Role-Based Access Control)                  │
└─────────────────────────────────────────────────────────────────┘

                            ┌──────────────┐
                            │  main.dart   │
                            │   (Entry)    │
                            └──────┬───────┘
                                   │
                    ┌──────────────┴──────────────┐
                    │                             │
            ┌───────▼─────────┐        ┌──────────▼─────────┐
            │ MultiProvider   │        │   MaterialApp      │
            │                 │        │                    │
            ├─ AuthProvider   │        ├─ initialRoute: /   │
            ├─ Dashboard      │        └────────────────────┘
            │  Provider       │
            └────────────────┘
                    │
         ┌──────────┴──────────┐
         │                     │
    ┌────▼─────────────┐  ┌───▼────────────────┐
    │  LoginScreenV2   │  │  RoleBasedRoute    │
    │                  │  │  (Protection)      │
    │ • Email field    │  │                    │
    │ • Password field │  │ • Checks role      │
    │ • Test creds btn │  │ • Allows/denies    │
    │ • Sign up link   │  │   access           │
    └────┬─────────────┘  └────────────────────┘
         │
         └──────────────┐
                        │
            ┌───────────▼──────────────┐
            │    AuthService           │
            │  (Singleton)             │
            │                          │
            │ • signIn()               │
            │ • signUp()               │
            │ • getRoleFromEmail()     │
            │ • Test credentials      │
            └───────────┬──────────────┘
                        │
        ┌───────────────┴────────────────┐
        │                                │
   ┌────▼──────────────┐      ┌──────────▼──────────┐
   │  Test Accounts    │      │  Supabase Database  │
   │  (In Memory)      │      │  (signin table)     │
   │                   │      │                     │
   │ admin@...         │      │ Stores:             │
   │ mgmt@...          │      │ • email             │
   │ acct@...          │      │ • password          │
   │ user@...          │      │ • full_name         │
   │                   │      │ • role              │
   └───────────────────┘      └─────────────────────┘
```

## User Role Hierarchy

```
┌─────────────────────────────────────────┐
│           User Role Hierarchy           │
└─────────────────────────────────────────┘

                    ADMIN
                   (👤 Highest)
                      │
           ┌──────────┼──────────┐
           │          │          │
       MANAGEMENT   ACCT      SERVICES
           │                      │
           └──────────┬───────────┘
                      │
                    USER
                (👤 Lowest)

Permissions by Role:
├─ ADMIN: All features + All reports
├─ MANAGEMENT: Inventory, Workers, Operations
├─ ACCOUNTANT: Finance, Reports, Analytics
└─ USER: Dashboard, Services, Personal data
```

## Authentication Flow Diagram

```
┌──────────────────────────────────────────────────────────┐
│              LOGIN AUTHENTICATION FLOW                   │
└──────────────────────────────────────────────────────────┘

User Input
    │
    │ (email, password)
    ▼
┌──────────────────────────┐
│  LoginScreenV2           │
│  • Validate inputs       │
│  • Call AuthService      │
└──────────┬───────────────┘
           │
           ▼
┌──────────────────────────────┐
│  AuthService.signIn()        │
│  • Check test credentials OR │
│  • Query Supabase database   │
└──────────┬──────────────────┘
           │
           ├─ Credentials invalid?
           │  └─> Return error
           │
           ├─ Credentials valid?
           │  └─> Proceed
           │
           ▼
┌──────────────────────────────┐
│  Determine User Role         │
│  • From email domain OR      │
│  • From database role field  │
│                              │
│  admin@... → UserRole.admin  │
│  mgmt@...  → UserRole.mgmt   │
│  acct@...  → UserRole.acct   │
│  user@...  → UserRole.user   │
└──────────┬──────────────────┘
           │
           ▼
┌──────────────────────────────┐
│  Create AuthUser             │
│  • id                        │
│  • email                     │
│  • fullName                  │
│  • role                      │
│  • createdAt                 │
└──────────┬──────────────────┘
           │
           ▼
┌──────────────────────────────┐
│  AuthProvider.setUser()      │
│  • Store user globally       │
│  • Notify listeners          │
│  • Clear errors              │
└──────────┬──────────────────┘
           │
           ▼
┌──────────────────────────────┐
│  Route Based on Role         │
│                              │
│  getHomeRoute(role):         │
│  • admin → /admin/dashboard  │
│  • mgmt → /mgmt/dashboard    │
│  • acct → /acct/dashboard    │
│  • user → /dashboard         │
└──────────┬──────────────────┘
           │
           ▼
┌──────────────────────────────┐
│  Navigate to Dashboard       │
│  • Show role-specific UI     │
│  • Display user name         │
│  • Enable features for role  │
└──────────────────────────────┘
```

## Access Control Flow

```
┌────────────────────────────────────────────────────┐
│       ROUTE ACCESS PROTECTION FLOW                 │
└────────────────────────────────────────────────────┘

User clicks on Screen/Route
    │
    ▼
Is User Authenticated?
    │
    ├─ NO → Show Loading or Login
    │
    └─ YES → Continue
            │
            ▼
       Is Screen Protected?
            │
            ├─ NO → Show content
            │
            └─ YES → Check Role
                    │
                    ├─ User role in allowedRoles?
                    │
                    ├─ YES → Show protected content ✓
                    │
                    └─ NO → Show Access Denied ✗
                           • Display lock icon
                           • Show message
                           • Provide Go Back button
```

## Component Interaction Diagram

```
┌─────────────────────────────────────────────────┐
│         COMPONENT INTERACTION MAP               │
└─────────────────────────────────────────────────┘

            AuthProvider (State)
                    ▲
                    │ notifies
                    │
    ┌───────────────┼───────────────┐
    │               │               │
    ▼               ▼               ▼
 Widgets      LoginScreenV2   RoleBasedRoute
   │                │               │
   │                │               │
   ├─ Check role    ├─ Get creds    ├─ Get allowed
   ├─ Get user      ├─ Call         │  roles
   ├─ Show/hide     │  AuthService  └─ Compare with
   │  features      │               │  user role
   │                └─ Set user     │
   │                   in Provider  └─ Show content
   │                                   or deny
   │
   │◄──────────────────────────────────┤
   │         AuthService (Logic)
   │
   ├─ Check credentials
   ├─ Query database
   ├─ Determine role
   ├─ Create AuthUser
   └─ Return result
       │
       ├─► Test Accounts (In Memory)
       │
       └─► Supabase Database (Persistent)
```

## Role-Based Features Matrix

```
┌────────────────────────────────────────────────────────────┐
│                  FEATURE ACCESS MATRIX                     │
└────────────────────────────────────────────────────────────┘

                 ADMIN  MGMT  ACCT  USER
Dashboard         ✓      ✓     ✓     ✓
Finance           ✓      ✗     ✓     ✗
Inventory         ✓      ✓     ✗     ✗
Waste Stock       ✓      ✓     ✗     ✗
Workers           ✓      ✓     ✓     ✗
Services          ✓      ✗     ✗     ✓
Profile           ✓      ✓     ✓     ✓
History           ✓      ✗     ✓     ✓
Rewards           ✗      ✗     ✗     ✓
Reports           ✓      ✓     ✓     ✗
Settings          ✓      ✗     ✗     ✗

Legend:
✓ = Has Access
✗ = No Access
```

## Data Flow Diagram

```
┌──────────────────────────────────────────────────┐
│              GLOBAL DATA FLOW                    │
└──────────────────────────────────────────────────┘

User Action (Login)
    │
    ▼
UI Event Handler
    │ (onPressed)
    ▼
AuthService.signIn()
    │
    ├─ Query credentials
    │  (test or database)
    │
    └─ Create AuthUser
        │
        ▼
    AuthProvider.setUser()
        │
        ├─ Update _currentUser
        ├─ Update _userRole
        ├─ Clear _errorMessage
        │
        └─ notifyListeners()
            │
            ▼
        All Consumers rebuild
            │
            ├─► Check role
            ├─► Show/hide features
            ├─► Enable/disable buttons
            │
            └─► Navigate to home screen
                based on role
```

## State Management Structure

```
┌─────────────────────────────────────┐
│      AuthProvider Structure         │
├─────────────────────────────────────┤
│ - _currentUser: AuthUser?           │
│ - _isLoading: bool                  │
│ - _errorMessage: String?            │
│                                     │
│ Methods:                            │
│ + setUser(AuthUser)                 │
│ + clearUser()                       │
│ + hasRole(UserRole): bool           │
│ + hasAnyRole(List<UserRole>): bool  │
│ + setLoading(bool)                  │
│ + setError(String?)                 │
│ + clearError()                      │
│                                     │
│ Getters:                            │
│ + currentUser: AuthUser?            │
│ + isLoading: bool                   │
│ + errorMessage: String?             │
│ + isAuthenticated: bool             │
│ + userRole: UserRole?               │
└─────────────────────────────────────┘
```

## Login Screen Components

```
┌──────────────────────────────────────────┐
│        LoginScreenV2 Layout              │
├──────────────────────────────────────────┤
│                                          │
│          [Recycling Logo]                │
│                                          │
│      "WasteFreeBD"                       │
│    "Role-Based Access"                   │
│                                          │
│  ┌──────────────────────────────────┐   │
│  │ [📧] Email Address               │   │
│  └──────────────────────────────────┘   │
│                                          │
│  ┌──────────────────────────────────┐   │
│  │ [🔒] Password          [👁️]      │   │
│  └──────────────────────────────────┘   │
│                                          │
│    [LOGIN BUTTON]                        │
│                                          │
│    [View Test Credentials] (link)        │
│                                          │
│  Don't have account? [Sign Up]           │
│                                          │
└──────────────────────────────────────────┘
```

---

This completes the full RBAC system architecture for WasteFreeBD! 🎉

