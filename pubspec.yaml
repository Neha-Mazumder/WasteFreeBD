name: wastefreebd
description: WasteFreeBD Flutter Application - Comprehensive waste management system
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.12.0
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  intl: ^0.20.2
  fl_chart: ^0.65.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.1
  geolocator: ^10.1.0
  image_picker: ^1.0.0
  image: ^4.0.17
  mobile_scanner: ^5.1.0
  permission_handler: ^11.3.0
  uuid: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
